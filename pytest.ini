[pytest]
addopts = -q
testpaths = service/tests
pythonpath = service

markers =
    integration: tests de integración que requieren servicio externo o mock de staging
    e2e: tests end-to-end con navegador (Selenium)
